CREATE TABLE Tabela
(
	Id int IDENTITY(1,1) NOT NULL,
	Nome varchar(255) NOT NULL,
	Cpf varchar(255) NOT NULL,
	Telefone varchar(255) NOT NULL,
	Endereco varchar(255) NOT NULL,
	dataNascimento date NOT NULL,
	Naturalidade varchar(255) NOT NULL,
	CONSTRAINT Pk_Tabela PRIMARY KEY (Id)
);

select * from Tabela;

insert into Tabela (Nome, Cpf, Telefone, Endereco, dataNascimento, Naturalidade) values ('joao', '123', '321', 'abc', '1/1/1', 'bc');



--update
BEGIN
select * from Tabela
update Tabela set Nome = 'joãoUPTADE', Cpf = '9999', Telefone = '123', Endereco = 'addaw', dataNascimento = GETDATE(), Naturalidade = 'adwdaw'
Where Id = 1
select * from Tabela

--delete
Where Id = 1
select * from Tabela
delete from Tabela where id = 1
select * from Tabela
end;

Where Id = 1
delete from Tabela where id = 1
